import type { Props } from "../components/footer/BackToTop.tsx";
import Component from "../components/footer/BackToTop.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
